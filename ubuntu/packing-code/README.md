# 说明
使用docker进行代码打包。需使用最新版本docker。
## 文件说明
| 文件 | 说明 |
|----|----|
| java-jar.sh | 将Java代码打包为jar文件，支持自定义代码目录，支持自定义JDK和maven版本。默认放在代码根目录执行。 |
| web-dist.sh | 打包前端代码，生成dist目录。将脚本放在代码目录的根目录，执行脚本。 |
## 使用方法
将脚本放在代码根目录，`bash *.sh` 执行脚本，自动打包。
