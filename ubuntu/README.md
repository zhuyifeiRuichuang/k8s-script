存放适用于ubuntu操作系统的脚本，无特殊标记的适用于所有ubuntu发行版。  
`download_packages.sh`,用于下载Ubuntu指定名称的软件包及依赖包，需在与离线主机同操作系统版本的联网主机操作，自带打包功能，打包文件复制到离线环境后解压可直接安装。  
`build-java-code.sh` 在任意系统可使用的脚本，依赖docker，基于ubuntu24容器镜像，可自定义JDK版本，自定义maven版本，对指定目录的Java代码打包，并将打包后的结果留在目录中，自动清理临时的容器。解决了开发工具配置多JDK和maven环境管理杂乱的问题。
